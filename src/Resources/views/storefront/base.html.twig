{% sw_extends '@Storefront/storefront/base.html.twig' %}

{% block base_body_script %}
    {{ parent() }}
    <script>
        window.elioSearch = {
            tracking: {
                detailPath: "{{ path('widgets.elio-search.tracking.product-detail') }}"
            }
        }
    </script>
{% endblock %}
