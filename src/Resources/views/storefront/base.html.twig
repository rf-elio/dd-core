{% sw_extends '@Storefront/storefront/base.html.twig' %}

{% block base_body_script %}
    {{ parent() }}
    <script>
        window.ff = {
            tracking: {
                detailPath: "{{ path('widgets.elio-ff.tracking.product-detail') }}"
            }
        }
    </script>
{% endblock %}
