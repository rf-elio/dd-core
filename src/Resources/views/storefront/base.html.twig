{% sw_extends '@Storefront/storefront/base.html.twig' %}

{% block base_body_script %}
    {{ parent() }}
    <script>
        window.elioDataDiscovery = {
            tracking: {
                detailPath: "{{ path('widgets.elio-data-discovery.tracking.product-detail') }}"
            },
            global: {
                active: "{{ config('ElioDataDiscovery.config.active') }}",
                searchUseElioDataDiscovery: "{{ config('ElioDataDiscovery.config.searchUseElioDataDiscovery') }}",
                listingUseElioDataDiscovery: "{{ config('ElioDataDiscovery.config.listingUseElioDataDiscovery') }}"
            }
        }
    </script>
{% endblock %}
